class ACE_Settings {
    class GVAR(destroyChance) {
        value = DESTROY_CHANCE_DEFAULT;
        typeName = "SCALAR";
        displayName = CSTRING(DestroyChance);
        description = CSTRING(DestroyChanceDesc);
    };
};
