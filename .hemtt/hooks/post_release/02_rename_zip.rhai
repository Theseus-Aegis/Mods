let orig = HEMTT_PROJECT_NAME + "-" + HEMTT_PROJECT_VERSION + ".zip";
let origlatest = HEMTT_PROJECT_NAME + "-latest.zip";

let newn = HEMTT_PROJECT_PREFIX + "_mods_" + HEMTT_PROJECT_VERSION + ".zip";
let newnlatest = HEMTT_PROJECT_PREFIX + "_mods_latest.zip";

print("Renamed " + orig + " to " + newn);
